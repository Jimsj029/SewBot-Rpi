# Theme configuration for SewBot - Futuristic Sci-Fi Style

# Color scheme - Different shades of blue with neon accents
COLORS = {
    # Main blues
    'dark_blue': (80, 40, 20),          # Deep dark blue (BGR)
    'medium_blue': (180, 100, 50),      # Medium blue (BGR)
    'bright_blue': (255, 180, 100),     # Bright blue (BGR)
    'cyan': (255, 255, 0),              # Cyan accent (BGR)
    'neon_blue': (255, 200, 0),         # Neon blue (BGR)
    
    # UI elements
    'button_normal': (200, 120, 40),    # Normal button color (BGR)
    'button_hover': (255, 180, 80),     # Hover state (BGR)
    'button_active': (255, 200, 100),   # Active/pressed state (BGR)
    
    # Text colors
    'text_primary': (255, 255, 255),    # White text (BGR)
    'text_secondary': (200, 200, 200),  # Light gray text (BGR)
    'text_accent': (255, 255, 0),       # Cyan text (BGR)
    
    # Background
    'bg_dark': (60, 30, 10),            # Dark background (BGR)
    'bg_overlay': (100, 50, 20),        # Overlay background (BGR)
    
    # Glow effects
    'glow_cyan': (255, 255, 0),         # Cyan glow (BGR)
    'glow_blue': (255, 150, 0),         # Blue glow (BGR)
}

# Font settings
FONTS = {
    'title_size': 2.5,
    'subtitle_size': 1.2,
    'button_size': 1.0,
    'small_size': 0.6,
}

# Animation settings
ANIMATION = {
    'glow_speed': 0.05,
    'pulse_speed': 0.03,
}
